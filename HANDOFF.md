# TeyPdfCad handoff

Date: 2026-09-18
Branch: feature/autocad-2022-arc-tessellation

## Repository

Root: C:\Users\Admin\Documents\ChatGPT\TeyConvert\TeyPdfCad-restored

The working tree is intentionally dirty. Preserve all existing user and assistant
changes; do not reset or clean the repository.

## Confirmed automated state

- `dotnet test tests/TeyPdfCad.Tests/TeyPdfCad.Tests.csproj --no-restore -c Release`: 44 passed.
- `dotnet test tests/TeyPdfCad.AutoCAD.Bridge.Tests/TeyPdfCad.AutoCAD.Bridge.Tests.csproj --no-restore -c Release`: 27 passed.
- AutoCAD plugin Release build with local AutoCAD 2022 API: 0 errors, 0 warnings.
- Latest verified plugin package before this handoff:
  `artifacts\autocad-test\20260918_225939`

## Implemented areas

- Native dimension semantic pipeline and validation.
- Arc/polyline tessellation and provenance preservation.
- Scale-aware A3 sheet bounds and title-block detection.
- Geometry-only title-block fallback for imported SHX linework with no text objects.
- AutoCAD commands: `TEYPDFPING`, `TEYPDFHEALTH`, `TEYPDFDUMPALL`,
  `TEYPDFRECONSTRUCTALL`, `TEYPDFSHEETCONFIG`, `TEYPDFSHEETAUDIT`.
- Bridge protocol v1, strict validation, status file handshake, cancellation and
  retention behavior.
- One-command scripts generated by `tools\prepare-autocad-test.ps1`:
  `RUN_A3_ACCEPTANCE.scr` and `RUN_FULL_A3_TEST.scr`.

## AutoCAD evidence

The user manually confirmed:

- plugin load and `TeyPdfCad health: ok`;
- real PDF import fixture: `selected=199`, `lines=2005`, `texts=0`, `INSUNITS=4`;
- measured sheet configuration: A3 420 x 297 mm, scale 100, MinX 2829.645,
  MinY 2065.104;
- an earlier writer version created and persisted
  `TeyPdfCad_A3_Landscape` and `TeyPdfCad_TitleBlock_Landscape` with
  `media=ISO_A3_(420.00_x_297.00_MM)`, `blockLines=700`, `lineweights=[25,60]`.

## Fixed source defect (2026-09-19, not yet runtime-accepted)

The `eNotInDatabase` failure on the paper-space viewport was diagnosed and fixed in
source. Two defects were corrected in `SheetLayoutWriter.cs`:

1. `Viewport.SetDatabaseDefaults()` was called before the viewport was resident in
   the database. It now runs only after `AppendEntity` +
   `AddNewlyCreatedDBObject`.
2. `LayoutManager.CreateLayout`/`DeleteLayout` were invoked while `Reconstruct`
   held an active transaction. `LayoutManager` is not transactional and its
   side effects left the surrounding transaction pointing at non-database object
   ids. Layout creation now goes through `LayoutDictionary` + `BlockTable`
   transactionally in the same transaction.

Verified before handoff: plugin Release build is 0 warnings/0 errors, Core 44,
Bridge 27, Web 24 tests pass. Runtime acceptance in real AutoCAD 2022 is still
outstanding; run the regenerated one-command script on a saved DWG and confirm the
full drawing is visible in Paper Space before claiming success.

## Other boundary

Core Console is not production/runtime-certified on this installation because
`C:\Program Files\Autodesk\AutoCAD 2022\acad2022.cfg` is missing. The user does
not want AutoCAD repaired; continue with ordinary GUI AutoCAD testing.

## Latest package paths

- Package: `C:\Users\Admin\Documents\ChatGPT\TeyConvert\TeyPdfCad-restored\artifacts\autocad-test\20260919_005235`
  - Acceptance script: `...\005235\RUN_A3_ACCEPTANCE.scr`
  - Full script: `...\005235\RUN_FULL_A3_TEST.scr`
  - Plugin DLL: `...\005235\plugin\TeyPdfCad.AutoCAD.dll`

The package may be stale after later source edits; regenerate it after any source
change using:

```powershell
dotnet build src/TeyPdfCad.AutoCAD/TeyPdfCad.AutoCAD.csproj --no-restore -c Release `
  "-p:TEYPDFCAD_AUTOCAD_LOCAL_API=C:\Program Files\Autodesk\AutoCAD 2022"

& .\tools\prepare-autocad-test.ps1 -SkipBuild -OutputRoot (Join-Path $pwd 'artifacts\autocad-test')
```
